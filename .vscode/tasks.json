{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build clientHTTP",
            "type": "shell",
            "command": "g++",
            "args": [
                "-std=c++11",
                "main.cpp",
                "-o",
                "${workspaceFolder}/clientHTTP",
                "-lcurl"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Build clientHTTP executable using g++"
        }
    ]
}
